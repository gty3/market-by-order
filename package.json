{
  "name": "market-by-order",
  "version": "1.0.0",
  "description": "Trading algorithms for market-by-order data",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1",
    "bundle": "esbuild algos/${npm_config_algo:-small-move-reversal}.ts --bundle --outfile=dist/${npm_config_algo:-small-move-reversal}-combined.js --format=iife"
  },
  "keywords": [
    "trading",
    "algorithms",
    "market-data"
  ],
  "author": "gty3",
  "license": "ISC",
  "devDependencies": {
    "esbuild": "^0.25.0",
    "typescript": "^5.0.0"
  }
}
